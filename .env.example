# Secret used to sign and verify JWTs. Set to a long random string.
JWT_SECRET=

# MongoDB connection string. Falls back to a local db if unset.
MONGODB_URI=

# Port the Express server listens on. Defaults to 8000 if unset.
PORT=
